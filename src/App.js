import React, { Component } from 'react';
import BookContainer from './containers/BookApp/BookAppContainer';

class App extends Component {
  render() {
    return (
      <BookContainer />
    );
  }
}

export default App;
